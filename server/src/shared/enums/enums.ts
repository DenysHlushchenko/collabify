export enum GenderType {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other',
}

export enum NotificationType {
  REQUEST = 'request',
  RESPONSE = 'response',
  LIKE = 'like',
  COMMENT = 'comment',
}
