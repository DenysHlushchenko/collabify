services:
    server:
        image: ${IMAGE}
        container_name: collabify-backend
        ports:
            - "5000:5000"
            - "5001:5001"
        env_file: .env.production

        restart: unless-stopped
